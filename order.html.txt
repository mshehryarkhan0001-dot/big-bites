<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Big Bites — Place Your Order</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Anton&family=DM+Sans:wght@400;500;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --bg: #17110c;
            --bg-alt: #201811;
            --cream: #fff3de;
            --cream-dim: #cabfa9;
            --mustard: #f5b301;
            --chili: #e8432d;
            --paper: #efe4c9;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            min-height: 100vh;
            background:
                linear-gradient(rgba(23,17,12,.92), rgba(23,17,12,.96)),
                url("https://images.unsplash.com/photo-1568901346375-23c9450c58cd?auto=format&fit=crop&w=1600&q=90");
            background-size: cover;
            background-position: center;
            color: var(--cream);
            font-family: "DM Sans", sans-serif;
        }

        h1, h2, h3 {
            font-family: "Anton", sans-serif;
            text-transform: uppercase;
            font-weight: 400;
            margin: 0;
        }

        .top {
            padding: 25px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1100px;
            margin: auto;
        }

        .logo {
            font-family: "Anton", sans-serif;
            font-size: 1.8rem;
        }

        .logo span {
            color: var(--chili);
        }

        .back {
            color: var(--cream);
            text-decoration: none;
            border: 1px solid rgba(255,243,222,.3);
            padding: 10px 18px;
            border-radius: 4px;
        }

        .back:hover {
            border-color: var(--mustard);
            color: var(--mustard);
        }

        .container {
            max-width: 850px;
            margin: 35px auto 70px;
            padding: 0 20px;
        }

        .heading {
            text-align: center;
            margin-bottom: 35px;
        }

        .heading small {
            color: var(--mustard);
            font-weight: 700;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .heading h1 {
            font-size: clamp(3rem, 8vw, 5.5rem);
            margin-top: 12px;
        }

        .heading p {
            color: var(--cream-dim);
            font-size: 1rem;
        }

        .order-box {
            background: rgba(239,228,201,.97);
            color: #2b2013;
            padding: 35px;
            border-radius: 8px;
            box-shadow: 0 25px 70px rgba(0,0,0,.55);
        }

        .order-box h2 {
            font-size: 2rem;
            color: var(--chili);
            margin-bottom: 25px;
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        .field {
            display: flex;
            flex-direction: column;
            gap: 7px;
        }

        .full {
            grid-column: 1 / -1;
        }

        label {
            font-weight: 700;
            font-size: .9rem;
        }

        input,
        select,
        textarea {
            width: 100%;
            border: 2px solid rgba(43,32,19,.15);
            background: white;
            color: #2b2013;
            padding: 13px 14px;
            border-radius: 4px;
            font-family: inherit;
            font-size: .95rem;
            outline: none;
        }

        input:focus,
        select:focus,
        textarea:focus {
            border-color: var(--mustard);
        }

        textarea {
            min-height: 110px;
            resize: vertical;
        }

        .submit-btn {
            width: 100%;
            margin-top: 25px;
            padding: 16px;
            background: var(--chili);
            color: white;
            border: 2px solid var(--chili);
            border-radius: 4px;
            font-size: 1rem;
            font-weight: 700;
            cursor: pointer;
            transition: .2s;
        }

        .submit-btn:hover {
            background: #b8301e;
            transform: translateY(-2px);
        }

        .submit-btn:disabled {
            opacity: .6;
            cursor: not-allowed;
            transform: none;
        }

        #message {
            display: none;
            margin-top: 20px;
            padding: 15px;
            border-radius: 4px;
            font-weight: 700;
            text-align: center;
        }

        .success {
            display: block !important;
            background: #d9f3dc;
            color: #155724;
        }

        .error {
            display: block !important;
            background: #f8d7da;
            color: #721c24;
        }

        .order-number {
            font-size: 1.2rem;
            color: var(--chili);
        }

        @media (max-width: 650px) {
            .form-grid {
                grid-template-columns: 1fr;
            }

            .full {
                grid-column: auto;
            }

            .order-box {
                padding: 22px;
            }

            .top {
                padding: 18px;
            }
        }
    </style>
</head>

<body>

<header class="top">
    <div class="logo">
        BIG <span>●</span> BITES
    </div>

    <a href="index.html" class="back">
        ← Back to Menu
    </a>
</header>

<main class="container">

    <div class="heading">
        <small>Fresh • Hot • Fast</small>
        <h1>Place Your Order</h1>
        <p>Fill in your details and we'll get your Big Bites order ready.</p>
    </div>

    <div class="order-box">

        <h2>Order Details</h2>

        <form id="orderForm">

            <div class="form-grid">

                <div class="field">
                    <label for="name">Your Name *</label>
                    <input
                        type="text"
                        id="name"
                        name="name"
                        placeholder="Enter your name"
                        required
                    >
                </div>

                <div class="field">
                    <label for="phone">Phone Number *</label>
                    <input
                        type="tel"
                        id="phone"
                        name="phone"
                        placeholder="0300 1234567"
                        required
                    >
                </div>

                <div class="field">
                    <label for="item">Select Food *</label>

                    <select id="item" name="item" required>

                        <option value="">
                            -- Select Food --
                        </option>

                        <option value="Big Bites Zinger">
                            Big Bites Zinger — Rs. 550
                        </option>

                        <option value="Smash Beef Burger">
                            Smash Beef Burger — Rs. 650
                        </option>

                        <option value="Cheese-Loaded Burger">
                            Cheese-Loaded Burger — Rs. 600
                        </option>

                        <option value="Crispy Wings (6pc)">
                            Crispy Wings (6pc) — Rs. 700
                        </option>

                        <option value="Chicken Broast (2pc)">
                            Chicken Broast (2pc) — Rs. 550
                        </option>

                        <option value="Zinger Roll">
                            Zinger Roll — Rs. 400
                        </option>

                        <option value="Shawarma Roll">
                            Shawarma Roll — Rs. 300
                        </option>

                        <option value="Loaded Fries">
                            Loaded Fries — Rs. 450
                        </option>

                        <option value="Cheesy Fries">
                            Cheesy Fries — Rs. 400
                        </option>

                        <option value="Regular Fries">
                            Regular Fries — Rs. 250
                        </option>

                        <option value="Oreo Shake">
                            Oreo Shake — Rs. 400
                        </option>

                        <option value="Mango Shake">
                            Mango Shake — Rs. 350
                        </option>

                        <option value="Soft Drink">
                            Soft Drink — Rs. 100
                        </option>

                    </select>
                </div>

                <div class="field">
                    <label for="quantity">Quantity *</label>

                    <input
                        type="number"
                        id="quantity"
                        name="quantity"
                        min="1"
                        max="20"
                        value="1"
                        required
                    >
                </div>

                <div class="field full">
                    <label for="address">Delivery Address *</label>

                    <input
                        type="text"
                        id="address"
                        name="address"
                        placeholder="Enter your complete delivery address"
                        required
                    >
                </div>

                <div class="field full">
                    <label for="notes">Special Instructions</label>

                    <textarea
                        id="notes"
                        name="notes"
                        placeholder="Extra cheese, less spicy, no onions, etc."
                    ></textarea>
                </div>

            </div>

            <button
                type="submit"
                class="submit-btn"
                id="submitBtn"
            >
                PLACE ORDER 🍔
            </button>

        </form>

        <div id="message"></div>

    </div>

</main>

<script>

const orderForm = document.getElementById("orderForm");
const submitBtn = document.getElementById("submitBtn");
const message = document.getElementById("message");

orderForm.addEventListener("submit", async function(event) {

    event.preventDefault();

    submitBtn.disabled = true;
    submitBtn.textContent = "PLACING ORDER...";

    message.className = "";
    message.style.display = "none";

    const orderData = {
        name: document.getElementById("name").value.trim(),
        phone: document.getElementById("phone").value.trim(),
        item: document.getElementById("item").value,
        quantity: document.getElementById("quantity").value,
        address: document.getElementById("address").value.trim(),
        notes: document.getElementById("notes").value.trim()
    };

    try {

        const response = await fetch(
            "http://localhost:5000/api/orders",
            {
                method: "POST",

                headers: {
                    "Content-Type": "application/json"
                },

                body: JSON.stringify(orderData)
            }
        );

        const result = await response.json();

        if (!response.ok) {
            throw new Error(result.message || "Order failed");
        }

        message.className = "success";

        message.innerHTML = `
            ✅ Order placed successfully!<br><br>
            <span class="order-number">
                Order #${result.order.id}
            </span>
            <br>
            Thank you ${result.order.name}! Your order has been received.
        `;

        orderForm.reset();

        document.getElementById("quantity").value = 1;

    } catch (error) {

        console.error(error);

        message.className = "error";

        message.innerHTML = `
            ❌ Order could not be placed.<br>
            Please make sure the Big Bites server is running.
        `;

    } finally {

        submitBtn.disabled = false;
        submitBtn.textContent = "PLACE ORDER 🍔";
    }

});

</script>

</body>
</html>